{
  "name": "@nexa/v6-graph-indexing",
  "version": "1.0.0",
  "private": true,
  "description": "Shared passive Nexa V6 subgraph implementation for Base, BSC and HyperEVM.",
  "scripts": {
    "codegen": "graph codegen subgraph.base.yaml",
    "codegen:all": "graph codegen subgraph.base.yaml && graph codegen subgraph.bsc.yaml && graph codegen subgraph.hyper-evm.yaml",
    "build:base": "graph build subgraph.base.yaml --output-dir build/base",
    "build:bsc": "graph build subgraph.bsc.yaml --output-dir build/bsc",
    "build:hyper-evm": "graph build subgraph.hyper-evm.yaml --output-dir build/hyper-evm",
    "build:all": "npm run build:base && npm run build:bsc && npm run build:hyper-evm",
    "test": "node --test test/*.test.mjs"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "0.98.1",
    "@graphprotocol/graph-ts": "0.38.2",
    "ethers": "6.15.0"
  },
  "dependencies": {
    "@nexa/solver-integration": "file:../.."
  }
}
