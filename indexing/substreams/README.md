# Nexa V6 Substreams

Shared event decoder and deterministic stores for Base, BSC, and
HyperEVM. The three generated manifests use the same WASM and protobuf model.
This package is non-authoritative and has no HTTP, RPC, database, signing, or
transaction path.
